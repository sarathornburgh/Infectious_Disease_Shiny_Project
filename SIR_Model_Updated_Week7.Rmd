install.packages("deSolve")
library(deSolve)
sir <- function(time, state, parameters) {
  with(as.list(c(state, parameters)), {
    dS <- -beta * S * I
    dI <- beta * S * I - gamma * I
    dR <- gamma * I
	
    return(list(c(dS, dI, dR)))
  })
}

init <- c(S = 1-1e-6, I = 1e-6, 0.0)
parameters <- c(beta = 1.4247, gamma = 0.14286)
times <- seq(0, 70, by = 1)
out <- as.data.frame(ode(y = init, times = times, func = sir, parms = parameters))
out$time <- NULL

# Change the parameters which include resistibility, infection, and susceptibility.

# Make a range of values for the parameter that only includes integers and does not include negative numbers.

# Use shiny apps to integrate our code and be able to allow the user to manipulate the parameters to their liking. 

# Allow the user to change the infection rate so that they can map any disease to this model.
